{
    "pluginAlias": "SwitcherBoiler",
    "pluginType": "accessory",
    "singular": true,
    "headerDisplay": "Homebridge plugin for Switcher - Boiler / Water-Heater",
    "footerDisplay": "Created by @nitaybz",
    "schema": {
        "type": "object",
        "properties": {
            "name": {
                "title": "Accessory Name",
                "description": "Name for your Switcher accessory",
                "type": "string",
                "default": "Boiler",
                "required": true
            },
            "accessoryType": {
                "title": "Accessory Type",
                "description": "Accessory Type and Icon - affect functionality (read more in the README.md file)",
                "type": "string",
                "default": "switch",
                "required": true,
                "oneOf": [
                  { "title": "Switch", "enum": ["switch"] },
                  { "title": "Outlet", "enum": ["outlet"] },
                  { "title": "Water Valve", "enum": ["valve"] }
                ]
            },
            "deviceId": {
                "title": "Device ID",
                "description": "Device ID for discovery - can only be extracted via scripts or you can view the ID in this plugin logs",
                "type": "string",
                "required": false
            },
            "ip": {
                "title": "Device IP Address",
                "description": "IP address of the device for discovery.",
                "type": "string",
                "required": false
            },
            "deviceName": {
                "title": "Switcher Device Name",
                "description": "Device exact name from Switcher app for discovery.",
                "type": "string",
                "required": false
            },
            "discoveryTimeout": {
                "title": "Discovery Timeout in Seconds",
                "description": "Maximum time (in seconds) to wait for device discovery.",
                "type": "integer",
                "default": 20,
                "required": false
            },
            "debug": {
                "title": "Enable Debug Logs",
                "description": "When checked, the plugin will produce extra logs for debugging purposes",
                "type": "boolean",
                "default": false,
                "required": false
            }
        }
    }
  }